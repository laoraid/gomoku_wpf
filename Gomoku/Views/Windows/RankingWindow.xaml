<Window x:Class="Gomoku.Views.RankingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gomoku.Views"
        mc:Ignorable="d"
        Title="RankingWindow" Height="450" Width="800"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:Gomoku.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:RankingViewModel}"
        Style="{StaticResource MaterialDesignWindow}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="랭킹" Style="{StaticResource MaterialDesignHeadline3TextBlock}" Margin="5"/>
            <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding RefreshCommand}">
                <materialDesign:PackIcon Kind="Refresh"/>
            </Button>
        </StackPanel>
        <Grid Grid.Row="1" VerticalAlignment="Stretch">
            <DataGrid ItemsSource="{Binding Rankings}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="순위" Binding="{Binding Rank}" Width=".5*" />
                    <DataGridTextColumn Header="닉네임" Binding="{Binding Nickname}" Width="2*" />
                    <DataGridTextColumn Header="승리" Binding="{Binding Wins}" Width="*" />
                    <DataGridTextColumn Header="패배" Binding="{Binding Losses}" Width="*" />
                    <DataGridTextColumn Header="무승부" Binding="{Binding Draws}" Width="*" />
                    <DataGridTextColumn Header="승률" Binding="{Binding WinRate}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         Width="50" Height="50" Value="0" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" IsIndeterminate="True"/>
        </Grid>
        <Button Grid.Row="2" Content="닫기" HorizontalAlignment="Right" Margin="10" Command="{Binding ConfirmCommand}"/>
    </Grid>
</Window>
