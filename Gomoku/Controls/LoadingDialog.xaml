<UserControl x:Class="Gomoku.Controls.LoadingDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gomoku.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{Binding ElementName=AppearanceElement, Path=(TextElement.Foreground)}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal" 
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{materialDesign:MaterialDesignFont}"
             
             xmlns:viewmodels="clr-namespace:Gomoku.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LoadingDialogViewModel}"
             mc:Ignorable="d">
    <StackPanel Margin="16">
        <TextBlock Text="{Binding Title}" FontSize="14" Style="{StaticResource MaterialDesignHeadline1TextBlock}"/>
        <TextBlock Text="{Binding Message}" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
        <ProgressBar Margin="16" HorizontalAlignment="Center"
          IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" />
        <Button HorizontalAlignment="Center"
          Command="{Binding CancelCommand}"
          Content="취소" IsCancel="True"
          Style="{StaticResource MaterialDesignFlatButton}" />
    </StackPanel>
</UserControl>
